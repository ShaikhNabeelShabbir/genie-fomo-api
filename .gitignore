node_modules/
dist/
scripts/lib/dist/
.env
.env.local
*.log
.DS_Store
*.tsbuildinfo

# build_directory.py output — regenerate with a fresh FOMO_TOKEN
raw/
data/wallets.json
__pycache__/

# Database dumps. A 102 MB binary in git history is permanent and unremovable
# without a rewrite, and a backup does not belong in the repo.
*.dump
genie-fomo-schema-*.sql
.cache/

# Supabase CLI scratch state (project ref, pooler host). Not source.
supabase/.temp/

# Output of scripts/acceptance_capture.sh
captures/

# Output of `wrangler deploy --dry-run --outdir dist`
worker/dist/
# wrangler dev scratch state
worker/.wrangler/

# Claude Code agent worktrees
.claude/
