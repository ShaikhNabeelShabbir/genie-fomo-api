# Reply to the Genie app team — fix request v3, per ask

> **Dated note, kept as sent (17 Sep 2026).** Superseded where it disagrees with `README.md` and
> `openapi.yaml` in this folder (19 Sep 2026): the vocabulary is version 14 (`fields-v14.json`
> replaces `fields-v11.json`); `logoUrl` IS served on the three `/tokens` routes; a 503 carries
> `Retry-After` 5 or 15 (60 on `/health` before its first snapshot), a fault of ours is a 500, and 429 means your own window only.

17 Sep 2026. One line per ask in the order of your §13. "Deployed" means live on v2 now;
"after migration" means the code is live and the database change is applied within the hour;
the vocabulary is now **version 11**, snapshot in `fields-v11.json`.

| # | Ask | Status | What changed, and where to look |
|---|---|---|---|
| 1 | **V1a** suspect value out of every total | deployed + after migration | `/positions`: suspect rows are excluded from `totalValueUsd`, `coverage.pricedPositions` and `share`; their value is in `suspectUsd` beside `unsellableUsd`; `partial: true, partialReason: price_suspect` (and the combined words). `/aum/now` and `/aum/history` hourly points carry `suspectUsd` and `unsellableUsd`; both are left out of `totalUsd`; `reason: price_suspect` when nothing else was priced. Rows keep `priceUsd`, `valueUsd` and `priceSuspect: true` so you can see what was excluded |
| 1 | **V1b** the missed cupseyy coin | deployed | Two causes: the relative check compared each row to the wallet's gross total, so two impossible prices hid each other, and the market-cap check needs a supply we did not hold for `313ya6V…`. Now the check iterates (largest row against the remaining base), a single position above $1B with unknown supply is suspect, supply falls back to `token_info`, and there is a fourth rule, `no_market_over_ceiling`, the one you described: a position worth more than ten times its pool's liquidity, or worth more than $1M with no pool anywhere, is suspect. Live after it: cupseyy `totalValueUsd` $2,324 with `suspectUsd` $2.92B over 3,901 rows; shahh $19,601 with $14.78M suspect; luckedhub $17,370 with $101.6B suspect. Every excluded row still shows its price and `priceSuspectReason`, so you can audit the rule; if it excludes a coin you can trade, tell us the mint and the pool |
| 1 | **V1c** `priceSource` | deployed, fills within ~3 h | It was never written by any loader. Every balance capture and re-price now records `pegged`, `token_info` or `token_prices`; fomo-reported prices record `fomo_reported_entry`. Existing rows fill as the 10-minute balance slices rotate through the roster |
| 1 | **V1d** rebuild for cupseyy, luckedhub, shahh | after migration | The `:25` build recomputes the last two hours; a manual full rebuild for the three runs right after the migration. Their history points from 17 Sep onward will show `suspectUsd` and a `totalUsd` under $1M |
| 2 | **X1a** swaps after 14 Sep 12:41 | deployed + after migration | Two causes: the Solana swap resolver was a hand-run script that was never scheduled, and the EVM one was added after the last successful nightly on 8 Sep. Both now run on the Worker every 15 minutes, newest transactions first, Solana through Helius, EVM through Bitquery. Expect `/trades` to reach today within the first hour and the backlog to drain from the head |
| 2 | **X1b** trades-feed age in `/health` | deployed | `feeds.swaps` (newest `wallet_swaps` row, stale after 6 h) and `staleFeeds` includes `swaps`. `feeds.trades` is now described as what it is: fomoapi trade records, load time |
| 2 | **X1c** `/events?kind=swap` | answered | It reads the same swap table, so it carries exactly what `/trades` carries; one keyset feed is the right way to read it once the writer has caught up |
| 3 | **X2** BNB/ETH/SOL-paid swaps valued | deployed + after migration | The daily-close job could never select an EVM quote asset (it required a swap tag EVM rows never carry); it now prices every quote asset with a Binance pair. The swap writer prices the money leg `pegged → daily close (7-day lookback) → market`, stores which arm won, and re-prices unvalued rows of the last 30 days at the end of every run. `trades[].valueSource` is `money_side_pegged`, `money_side_daily_close` or `money_side_market` |
| 4 | **N1** ETH and BNB priced | deployed + after migration | Native coins reach `/aum/now` through the same daily close the swaps use (the live ladder lacked that rung), `/positions` through the balance re-price, and `/portfolio` through the wrapped-coin price that the native row was shadowing. If Binance refuses Cloudflare's egress the fallback is DexScreener's WETH/WBNB pairs; we will confirm on the first run |
| 5 | **Z1b** no `$0` from nothing priced | after migration | A zero price counted as priced, and a sub-cent total was rounded to `0.00` in storage. Prices `<= 0` are unpriced everywhere, totals are stored unrounded and rounded at the route, `pricedPositions` counts only positive values. `gmgn_0xcb4d28c2` will read either a real sub-cent value or `null, no_prices`, matching `/positions` |
| 5 | **R5b** 0x314e6555's coin | after migration | Same fix; his newest valued point will be non-zero or `null` with a reason. Why it lost its price between 16 and 17 Sep: the daily price row aged out of the 7-day lookback and GMGN returned nothing; the re-price now records the source so the next drop is explainable from the row |
| 6 | **T1** stale scorecards reloaded | deployed + after migration | Nothing had attempted the 16 since the nightly died on 14 Sep; the 6-hourly job's first run was 06:00 UTC. The job now targets `max(trades.captured_at)` older than 72 h, the same clock your `staleness` reads, retries at most every 6 h, and records every attempt: `loadAttemptedAt`, `loadOutcome` (`loaded`, `unchanged`, `degraded`, `unavailable`, `error`). `nextLoadAt` is the next 6-hourly tick, `nextLoadBasis: six_hourly_slot`. `/health` adds `scorecardNeverAttempted` and counts never-attempted as failed |
| 6 | **T3** fallback from the full swap store | deployed | `staleness.fallback: on_chain` only when the swap store covers at least half of the profile's swap count; otherwise `fallback: null, fallbackReason: swap_store_incomplete`. It becomes `on_chain` for smokey0x as X1 fills the store |
| 7 | **X3** the real `/aum` stop | answered | Sampling stopped **17 Sep 2026 at about 03:52 UTC**. Every "18 Sep" in the handoff was our error; the bundle is corrected. `/aum` still answers its stored readings and will not grow |
| 8 | **H1 / V2** handles | answered | `Lasercat397` is the display name of trader **`397397`**; the API resolves handle, display handle, id and `trd_<uuid>`, never the name, on v1 and v2 alike. `trancey` is not in the directory and never was. Use `397397` for both re-checks |
| 8 | **G2** `logoUrl` | deployed + after migration | `logoUrl` (nullable) on `/tokens/:address`, `/tokens` rows and `/tokens/momentum` rows, from GMGN, with DexScreener's image as the fallback filled by the hourly price run |
| 8 | Wallet-submission secret | pending, ours | The route refuses with 503 `not_configured` until the secret is set on the Worker; it is being set now. The diff is corrected to say so |
| 9 | **P1** | answered, doc corrected | `openPositions` still counts trade records (basis `trade_records`); `openPositionsHeld` is the figure that matches `/positions`. The handoff's "counts what `/positions` lists" was wrong; read `openPositionsHeld`, as you already do |
| 10 | **R6** coverage per chain | deployed | `coverage.chains.<chain>` is written by every balance slice from Bitquery's transaction count for the wallet (realtime window, so a lower bound; `basis: bitquery_realtime`) and the rows we hold; `partialReason: indexer_coverage_low` when the share is low. Fills as the slices rotate |
| 11 | **L2** | partly | `/fields` is cached five minutes. Stored single answers are 1.4–2.3 s because of query cost, not the Worker; index work continues separately |
| 11 | **B3** | closed | A retry inside the same hour overwrites the refused reading in the readings table, so no route can show it, and sampling has stopped; `/aum/history` hours that cannot be valued carry `reason` |

**New words in version 11**, all in `fields-v11.json`: `positions.partialReason` `price_suspect` and its
combinations; `positions[].priceSource` `pegged`, `token_info`, `token_prices`, `fomo_reported_entry`;
`positions.coverage.chains.*.basis` `bitquery_realtime`; `aumHistory.points[].reason` and
`aumHistory.now.reason` `price_suspect`; `trades[].valueSource` three words; `scorecard.loadOutcome`
`unchanged`; `scorecard.nextLoadBasis` `six_hourly_slot`; `scorecard.staleness.fallbackReason`
`swap_store_incomplete`; `health.staleFeeds[]` `swaps`.

**New fields**: `suspectUsd` (positions, `/aum/now`, hourly history points), `unsellableUsd` on the
same two AUM routes, `coverage.suspectPositions`, `logoUrl`, `feeds.swaps`, `scorecardNeverAttempted`,
`scorecardStaleGmgn`, `fallbackReason`, `cachedForSeconds` on `/fields`.
